import * as React from 'react';

import Landing from 'client/ui/components/landing';

export default class Home extends React.Component {

    public render (): React.ReactNode {
        return (
            <Landing/>
        );
    }
}
